import { TipoMaterialPipe } from './tipo-material.pipe';

describe('TipoMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
